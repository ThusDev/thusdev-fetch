# Migration

## 1.2 to 1.3

- Redirects now default to `error`.
- Security validation is available through `security` options.
- Request and response size limits are available.
- Cache storage is bounded.
- `deadline` provides a total request deadline across retries.
